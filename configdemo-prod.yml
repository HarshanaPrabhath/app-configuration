build:
  id: 201201
  version: 1.2.0.0
  name: "PROD-GIT-Production-build"
  type: "GIT Production Build"


  management:
    endpoint:
      refresh:
        enabled: true
    endpoints:
      web:
        exposure:
          include: refresh
